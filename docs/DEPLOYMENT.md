**Task**

* Deploy the model to a remote GitHub repository for public showcasing with a comprehensive `README.md`.

**Context**

* File and directory paths: `./src`, `./docs`, `./models`, `pyproject.toml`, `./pipeline`, and `README.md`.

**Constraints**

* Do not commit any hidden files to the repository.
* Do not read over any other .MD files besides this one. 
* Do not commit any files or directories not listed in the context section.
* Do not develop any new code for the modeling solution.

**Directions**

1. Read over all resources listed in the context section and the `.gitignore` file.
2. Develop a comprehensive `README.md` covering:
* What the project is
* What data was used
* Steps for model selection
* Training and validation findings
* How to set up the environment to train the model
* Visuals associated with the exploring, training, and validation steps
* Contact information
* Licensing


3. Remove unnecessary comments in files and execute `black` on any `.py` files to follow PEP formatting rules.
4. Create a commit message structured as `Branch Name: Action taken`.
5. Commit and push the changes to the remote repository.

**Output**

* Forecasting model project ready for public viewing.